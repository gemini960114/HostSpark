# Secrets and Environment Variables (機密資訊，絕對排除)
.env
*.env
*.pem
*.key
*.token
schedules.db
schedules.db-wal
schedules.db-shm

# Python Virtual Environments
venv/
.venv/
env/
ENV/
.security-tools-venv/

# Security scan reports (may contain secret snippets found by gitleaks -- never commit)
security-reports/
# A dated, file:line-specific scan writeup -- useful locally, but stays private:
# it doubles as a map of "what a scanner once flagged here" if the code regresses later.
SECURITY-SCAN-REPORT.md

# Python Cache & Bytecode
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Logs
*.log
.coverage
.pytest_cache/

# OS / Editor metadata
.DS_Store
Thumbs.db
.vscode/
.idea/
